import { StyleSheet } from "react-native";

export const buttonStyles = StyleSheet.create({
  tinyButton: {
    width: 16,
    height: 8, 
  },
  mediumButton: {
    width: 128,
    height: 32, 
  }
});